# FastAPI and server
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Geospatial libraries
geopandas>=0.14.0
pyogrio>=0.5.0
rasterio>=1.3.9
xarray>=2023.10.0
rioxarray>=0.16.0

# Data processing
pandas>=2.1.0
numpy>=1.24.0

# Image processing for tiles
pillow>=10.1.0
matplotlib>=3.8.0
seaborn>=0.13.0
contextily>=1.4.0

# Caching
cachetools>=5.3.0

# --------------------------------------------------------------------------- #
# AI assistant (app/assistant/)
#
# Optional: without these the assistant router is not mounted and the app runs
# exactly as before. matplotlib / seaborn / contextily above are reused for the
# assistant's figures.
# --------------------------------------------------------------------------- #
anthropic>=1          # Claude provider
google-genai          # Gemini provider
mcp>=2                # MCP server grafted at /mcp
python-docx           # Word report generation
openpyxl              # Excel export
python-dotenv         # backend/.env with API keys; was already installed

# Optional: For vector tiling/optimization (mentioned in requirements)
# lonboard>=0.1.0  # Uncomment if needed for large dataset rendering

