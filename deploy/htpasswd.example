# Generate a real htpasswd file (do not commit secrets):
#   apt-get install -y apache2-utils   # or: apk add apache2-utils
#   htpasswd -c deploy/htpasswd analyst
# Then rename/copy to deploy/htpasswd (compose mounts that path).
#
# Example line format (bcrypt):
# analyst:$apr1$example$hashnotreal
